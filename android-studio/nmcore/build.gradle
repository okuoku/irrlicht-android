apply plugin: 'com.android.library'

android {
    compileSdkVersion 28
}

sourceSets {
    main {
        java {
            srcDir '../../packaging/java'
        }
    }
}

dependencies {
    api project(path: ':sdl', configuration: 'default')
}